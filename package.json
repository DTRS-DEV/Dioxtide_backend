{
  "name": "dioxtide-backend",
  "version": "1.0.0",
  "description": "DIOXTIDE auth backend — signup/login with bcrypt-hashed passwords, admin-only user list.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
